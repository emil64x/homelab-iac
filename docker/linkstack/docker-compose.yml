services:

  linkstack:
    hostname: 'linkstack'
    image: 'linkstackorg/linkstack:latest'
    user: "1001:1001"
    environment:
      TZ: 'Europe/London'
      LOG_LEVEL: 'info'
      PHP_MEMORY_LIMIT: '256M'
      UPLOAD_MAX_FILESIZE: '8M'
    volumes:
      - '${STORAGE}/linkstack_data:/htdocs'
    ports:
      - '8490:80'
    restart: unless-stopped
